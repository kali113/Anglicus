{
  "name": "anglicus-desktop",
  "version": "0.6.0-alpha",
  "description": "Anglicus Desktop Wrapper",
  "private": true,
  "main": "lib/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && electron .",
    "dev": "npm run build && cross-env ANGLICUS_URL=https://kali113.github.io/Anglicus/ electron .",
    "dist": "npm run build && electron-builder",
    "dist:win": "npm run build && electron-builder --win nsis"
  },
  "devDependencies": {
    "@types/node": "^22.13.1",
    "cross-env": "^7.0.3",
    "electron": "^35.7.5",
    "electron-builder": "^26.7.0",
    "typescript": "^5.9.3"
  },
  "build": {
    "appId": "io.github.kali113.anglicus",
    "productName": "Anglicus",
    "asar": true,
    "directories": {
      "output": "release"
    },
    "files": [
      "lib/main.js",
      "assets/**",
      "package.json"
    ],
    "win": {
      "target": "nsis",
      "icon": "assets/icon.png"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "Anglicus"
    }
  }
}
